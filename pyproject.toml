[project]
name = "voice-call-analyzer"
version = "0.1.0"
description = "Voice call analyzer using DeepAgents + TFY AI Gateway + MCP"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.1.0",
    "langchain-mcp-adapters>=0.1.0",
    "langchain-openai>=0.3.0",
    "langgraph-cli[inmem]>=0.4.19",
    "python-dotenv>=1.0.0",
]
